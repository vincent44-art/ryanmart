# Production Environment Variables
# This file is used when running `npm run build` for production deployment

# API Base URL for DreamHost production
# The DreamHost server is configured to serve the frontend and the backend under `/api`.
# Set the base URL to the frontend-accessible backend mount point. Do NOT include a trailing slash.
REACT_APP_API_BASE_URL=https://ryanmart.store/api

# Note: When running `npm run build`, this environment variable will be injected
# into the built JavaScript files at build time. Make sure to set this before building.
