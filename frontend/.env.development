# Development environment variables
# Point the frontend to the local backend to avoid production CORS during development.
REACT_APP_API_BASE_URL=http://localhost:5000/api

# Allow running on arbitrary hostnames in dev if needed
DANGEROUSLY_DISABLE_HOST_CHECK=true
