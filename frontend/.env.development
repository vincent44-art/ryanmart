# Local Development Environment Variables
# This file is used when running `npm start` for local development

# By default local development points to a local backend server at
# http://localhost:5000. If you want to test against the remote
# DreamHost backend, uncomment the REACT_APP_API_BASE_URL line below.

# Local backend (recommended for offline dev)
REACT_APP_API_BASE_URL=http://localhost:5000/api

# Remote DreamHost backend (uncomment to use remote during local dev)
# REACT_APP_API_BASE_URL=https://ryanmart.store/api

# Optional: Debugging flags
# REACT_APP_DEBUG=true
